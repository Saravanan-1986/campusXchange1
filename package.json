{
  "name": "campusxchange",
  "version": "1.0.0",
  "private": true,
  "description": "College-exclusive academic resource & knowledge sharing platform — a 5-database-paradigm showcase (MongoDB, Neo4j, Temporal, Active, Spatial)",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
